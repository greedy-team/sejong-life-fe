import type { Review } from '../../features/placeDetail/PlaceDetailContainer';

interface ReviewCardProps {
  review: Review;
}

const ReviewCard = ({ review }: ReviewCardProps) => {
  return <></>;
};

export default ReviewCard;
